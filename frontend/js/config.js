/* App configuration.
   The API base URL is normally set at runtime (Setup screen) and stored in
   localStorage as 'spa_api'. You can pre-fill it here if you prefer. */
window.AppConfig = {
  apiBase: '', // e.g. 'https://script.google.com/macros/s/AKfyc.../exec'
  defaultPlantId: ''
};
